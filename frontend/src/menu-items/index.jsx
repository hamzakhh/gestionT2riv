// project import
import dashboard from './dashboard';
import association from './association';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, association]
};

export default menuItems;
